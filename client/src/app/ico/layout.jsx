export const metadata = {
  title: 'Pixora | Favicon Converter ',
  description: 'Create favicon.ico files from PNG, JPG, WebP, SVG and AVIF images in seconds. Convert images to ICO favicon files online. Fast, free and secure.'
};

export default function Layout({ children }) {
  return children;
}