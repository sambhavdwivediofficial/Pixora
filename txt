

cd src-tauri
cargo run --bin pixora-server


cd src-tauri
cargo run --bin pixora-server --features desktop

cargo install tauri-cli
cargo tauri dev

cargo build --release --bin pixora-server


# Development

cargo tauri dev

# Frontend (Next.js) aur Tauri window dono ek saath start hoti hai. Code change karo, window live reload hoti hai. Sirf development ke liye.

# Production Build

cargo tauri build

# Ek complete installable app banta hai — installer, .exe, .dmg jo bhi OS hai uske hisaab se.


# Sirf Rust compile check (bina run kiye)

cargo build

# Errors check karne ke liye. App nahi chalta, sirf compile hota hai.

# Sirf standalone HTTP server (bina window ke)

cargo run --bin pixora-server

# Sirf backend port 8599 pe. Koi window nahi. Python backend ki tarah.

cargo install tauri-cli --version "^2"

# App Build Hone ke Baad Path aur Naam

# Windows
src-tauri/target/release/bundle/msi/Pixora_1.0.0_x64_en-US.msi
src-tauri/target/release/bundle/nsis/Pixora_1.0.0_x64-setup.exe


# macOS
src-tauri/target/release/bundle/dmg/Pixora_1.0.0_x64.dmg
src-tauri/target/release/bundle/macos/Pixora.app


# Linux
src-tauri/target/release/bundle/deb/pixora_1.0.0_amd64.deb
src-tauri/target/release/bundle/appimage/pixora_1.0.0_amd64.AppImage
Naam tauri.conf.json ke productName aur version se aata hai — jo humne Pixora aur 1.0.0 rakha hai.